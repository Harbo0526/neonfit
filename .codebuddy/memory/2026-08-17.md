# 2026-08-17

## 清空用户数据需求
用户要求把全部用户数据清空，重新注册试验。已处理：
- 本地 `data/users.json` 清空为 `{"users":[]}`；`harbo.json`、`harbo2.json` 数据清空（保留空结构）。
- 用户数据三处分布：① 本地 `data/` 目录；② 云端 GitHub `data/`（含遗留大写 `Harbo.json` 旧数据）；③ 浏览器 localStorage（`neonfit_gh_token` + `neonfit_<user>_*`）。
- 等待用户决定：A) 在登录页加正式「清空所有用户数据」按钮（一键清本地+云端）；B) 手动控制台方式（已提供两段 JS）不动代码。
- 注意：曾遗留 GitHub 大写 `Harbo.json` 与小写 `harbo.json` 并存（大小写敏感、独立文件），用户要求保留大小写兼容。
