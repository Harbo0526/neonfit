# CHANGELOG · HarboFit 运动打卡

版本规则：主版本.功能版本.补丁号（如 v1.0.0）。每次修改递增对应数字，打上版本号与日期（年-月-日），同一天的修改合并到一条、不重复打时间戳。

---

## v1.0.2 · 2026-08-17
- **PWA 更新机制修复（手机端不更新问题）**：`sw.js` 缓存策略由 cache-first 改为 network-first，确保每次拉取 GitHub 最新版；`CACHE` 版本号改为 `neonfit-v3`（发版需手动 +1，文件顶部已加醒目注释提醒）。新增「🔄 发现新版本，点击立即刷新」提示条，检测到新 SW 等待激活时弹出，点击即接管并自动 reload，无需手动清缓存。
- **修复清空全部不同步到云端（致命 bug）**：`cloudSync()` 原 `_syncing` 互斥锁会静默丢弃并发同步请求，导致连续清空时仅首次生效、GitHub 残留旧数据。改为「脏标记排队」模型，期间有新变更会自动补同步一次，保证最终一致；清空按钮改为一次性清空本地后只触发一次 `cloudSync()`。
- **历史记录支持长按单条删除**：每行 `<tr>` 绑定 600ms 长按（鼠标/触摸通用），弹出 `confirm` 确认后删除该条记录；原有「✕」按钮也加确认弹窗防误删。
- **标题栏显示当前用户名**：右上角新增「👤 用户名」胶囊标签，登录/恢复 session 后自动展示。
- **备份文案优化**：历史页「导出 JSON / 导入 JSON」按钮改为「⬇ 备份下载 / ⬆ 备份上传」，导出后提示同步为「已备份下载」。
- **品牌更名 NEON FIT → HarboFit**：登录页、侧边栏、404、PWA 名称（name/short_name）、苹果触摸标题、日期后缀、下载文件名（harbofit_日期.json）、README/CHANGELOG 标题、内部更新消息常量（HARBOFIT_UPDATE）全部统一。GitHub 仓库名 `neonfit` 与 localStorage key 前缀（`neonfit_`）保持不变，避免老用户数据丢失。
- **用户注册查重 + 明文用户索引**：新增 `data/users.json` 索引文件，结构 `{"users":[{"user","pass"(明文),"createdAt"}]}`。注册时先查索引，用户名已存在则拦截（大小写不敏感）；未注册则追加明文记录并创建个人数据文件。登录改从索引校验密码，可在 GitHub 仓库直接查看各用户名及其对应密码（用户明确要求明文、接受公开仓库可见）。
- 顺手修复原有 bug：`DEF_TYPES` 在全局作用域被重复声明（会导致内联脚本运行时中断），已删除重复定义。

## v1.0.1 · 2026-08-17
- **移除硬编码仓库令牌**：原 `CONFIG.token` 明文写入导致 GitHub Push Protection（密钥扫描）拦截推送。改为登录页新增「仓库令牌」输入框，令牌仅存于用户浏览器 localStorage，不进入代码与公开仓库，推送不再被拦。
- README「多用户」章节同步更新令牌填写方式。

## v1.0.0 · 2026-08-17
- **重命名为 v1 基线版本**（此前内部迭代 v1~v5 合并计入本版）。
- **多用户 + GitHub 云端存储**：新增账号密码登录/注册页，每个用户数据隔离存于 GitHub 仓库 `data/<用户名>.json`。
- 首次注册自动在仓库创建用户云端文件；登录时从云端拉取数据；每次打卡本地缓存 + 异步写回云端（离线可用、不怕清缓存丢）。
- 密码采用前端简单哈希（FNV-1a 8 位十六进制，非加密级）。
- 顶部新增 `CONFIG`（owner/repo/token/branch）供部署填写；侧边栏新增「退出登录」按钮与当前用户名。
- 版本号机制：新增 `APP_VERSION` 常量，前端登录页与侧边栏展示版本号；建立 `CHANGELOG.md` 记录每次修改。
- 云端后端由早期 Gitee 方案改为 GitHub（Gitee Pages 已实质暂停），统一用 GitHub Pages 托管 + GitHub API 存储。

## v0.4.0 · 2026-08-14（历史摘要）
- 首页新增体重记录项；数据看板「运动类型分布」改为「体重曲线」。
- 打卡运动类型改为可自定义输入（datalist，非固定下拉）。
- 存储层新增体重与自定义类型持久化；导出/导入/清空结构同步扩展。

## v0.3.0 · 2026-08-14（历史摘要）
- 新增「导入 JSON」按钮，配合导出实现备份/恢复（按 id 去重合并）。

## v0.2.0 · 2026-08-14（历史摘要）
- 增加目标设定、周/月统计切换、PWA 离线缓存（manifest data URI + sw.js）。

## v0.1.0 · 2026-08-14（历史摘要）
- 初版：纯静态单文件运动打卡，霓虹 UI、粒子背景、localStorage、自研 NeonChart、响应式（移动底部 Tab / PC 侧边栏）。
