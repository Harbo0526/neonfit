# 运动打卡网站（NEON FIT）· 项目需求与目标总结

> 本文档用于向新对话窗口交接项目背景、目标与已完成任务。最后更新：2026-08-17

---

## 一、项目概述

- **项目名称**：NEON FIT 运动打卡（纯静态单文件 Web 应用）
- **定位**：个人运动打卡工具，主打赛博朋克/霓虹视觉风格，PC 与手机响应式，完全离线可用。
- **技术形态**：单文件 `index.html`（HTML+CSS+JS 全部内联），配套独立 `sw.js`（PWA 必需）。
- **数据存储**：浏览器 `localStorage`（不上云、无后端、无账号体系）。
- **目标用户**：以手机浏览器为主的个人用户。

---

## 二、核心需求（原始诉求）

1. **多页面结构**：首页、打卡页、数据看板、历史记录 4 个页面。
2. **UI 风格**：赛博朋克/霓虹风格，带粒子背景 + CSS 入场动画。
3. **数据存储**：使用 `localStorage` 持久化数据。
4. **图表展示**：集成图表展示运动数据（折线/环形/柱状）。
5. **响应式布局**：移动端底部 Tab 栏，PC 端侧边栏。
6. **离线单文件**：所有资源内联到单个 HTML，离线可用。

---

## 三、已完成的任务（迭代记录）

### v1 — 基础版
- 单文件 `index.html`，自包含、双击 `file://` 即用。
- 4 页面 SPA 切换（首页/打卡/看板/历史）。
- Canvas 粒子背景（霓虹星座连线，自适应 DPR）。
- CSS 入场动画（页面上浮 `pageIn`、卡片错峰 `rise`）。
- `localStorage` 存储打卡记录 `{id, type, duration, calories, distance, date, note}`。
- 内联轻量霓虹图表引擎（替代 CDN 版 Chart.js，满足离线+单文件），含：近 7 天消耗趋势（折线）、运动类型分布（环形）、时长对比（柱状）。
- 响应式：≤768px 切底部 Tab，PC 侧边栏。
- 历史记录：导出 JSON、清空、单条删除。

### v2 — 目标设定 + 周月切换 + PWA
- **目标设定**：周次数/周时长/月消耗/月次数，存 `neonfit_goal`，首页进度条 + 看板联动。
- **周/月统计切换**：看板分段控件，概览、目标、图表（近 7 天 ↔ 近 30 天）随范围重算。
- **PWA 离线缓存**：
  - `manifest` 运行时 `data:` URI 内联（零额外文件）。
  - `sw.js` 独立文件（浏览器强制要求），cache-first + 动态回填。
  - `beforeinstallprompt` 安装横幅。
  - `file://` 直接打开自动跳过 SW，仍可用。
- 修正：`fmtDate` 改用本地日期格式化（修复时区边界误差）。

### v3 — 导入备份（防清缓存丢数据）
- 历史记录页新增「⬆ 导入 JSON」+ 隐藏 file input。
- 导入按记录 `id` 去重合并（已存在跳过，不覆盖新数据）。
- 应对手机清缓存/卸载 App 导致数据丢失的兜底方案。

### v4 — 体重记录 + 自定义运动名称
- **首页体重**：卡片显示最新体重；「⚖ 体重记录」面板含迷你曲线 + 较首次增减。
- **看板替换**：「运动类型分布」环形图 → 「体重变化曲线」(chartWeight)，按周/月切换。
- **运动名称可自定义**：由固定 `select` 改为 input + datalist；常用类型存 `neonfit_types_v1`，打卡自动追加新名称；tag 颜色按名称 hash 动态生成 (`colorFor`)。
- **体重存储** `neonfit_weight_v1`；打卡表单加「今日体重 + 体重记录日期」。
- 导出/导入/清空覆盖体重与类型：导出结构 `{records, weight, types}`；导入按日期去重合并体重、合并类型；清空同时清三者。

---

## 四、关键设计决策与权衡

| 决策点 | 选择 | 原因 |
|---|---|---|
| 图表库 | 自研内联 canvas 引擎（非 Chart.js CDN） | 满足"离线+单文件"硬要求；CDN 破坏离线 |
| PWA manifest | `data:` URI 内联 | 零额外文件，保持单文件 |
| PWA service worker | 独立 `sw.js` | 浏览器强制要求独立文件，无法内联 |
| 数据持久化 | `localStorage` | 纯前端、零后端、离线 |
| 防丢数据 | 导出/导入 JSON | `localStorage` 随清缓存丢失，需用户主动备份 |
| 运动名称 | 可自定义 input + datalist | 用户要求不限定固定下拉 |

---

## 五、数据存储结构

| Key | 内容 | 说明 |
|---|---|---|
| `neonfit_records_v1` | 打卡记录数组 | `{id, type, duration, calories, distance, date, note}` |
| `neonfit_weight_v1` | 体重记录数组 | `{date, weight}`，按日期去重 |
| `neonfit_types_v1` | 常用运动名称数组 | 用于 datalist 建议 |
| `neonfit_goal` | 目标设定 | `{weeklyCount, weeklyDuration, monthlyCalories, monthlyCount}` |
| `neonfit_install_dismiss` | 安装横幅已关闭标记 | 仅 UI 状态 |

---

## 六、打开与访问方式

- **本地预览服务**（本机会话内启动，端口随版本变化：v1 8137 / v2 8139 / v4 8141，进程可能退出需重启）：
  - 脚本：`C:\Users\admin\AppData\Local\Temp\neonfit_serve.py`（`chdir` 到中文目标目录后 `http.server`）。
  - 启动：`Start-Process python -ArgumentList "C:\Users\admin\AppData\Local\Temp\neonfit_serve.py"` → 访问 `http://localhost:8141/index.html`。
  - 当前有效端口：**8141**（若进程退出，重启后同端口）。
- **双击文件**：`d:\Desktop\03工作文档资料\20260814 古城项目\运动打卡\index.html`（`file://`，离线可用但不可安装 App、不注册 SW）。
- **真·PWA**：需 `http://localhost` 或 HTTPS 访问才能注册 SW、可安装离线 App。
- ⚠️ **重要坑**：双击 `file://` 与 `localhost` 是两套独立存储，勿混用，否则数据"看似丢失"其实是存在另一套。

---

## 七、已知限制与风险

1. **数据随浏览器清缓存/卸载 App 丢失** —— 已用导入导出兜底，但需用户主动备份。
2. **换设备/换浏览器数据不通** —— 纯本地存储，无云同步。
3. **服务器进程会话内可能退出** —— 预览服务非持久，需手动重启。
4. **同一域名多源** —— `file://` 与 `localhost` 不互通。

---

## 八、可能的后续增强（待定）

- [ ] 体重目标线（曲线上画目标体重参考线）
- [ ] 首页体重卡片显示「距目标还差 X kg」
- [ ] 自动备份到文件下载（每次打卡落盘）
- [ ] 云同步（账号体系，需联网+后端，与离线定位相反）
- [ ] IndexedDB 替代 localStorage（容量更大更稳）
- [ ] 真正内联 Chart.js 源码（若接受文件体积增大）

---

## 九、文件清单

```
运动打卡/
├── index.html                        # 主文件（自包含：HTML+CSS+JS+图表引擎+PWA逻辑）
├── sw.js                             # Service Worker（PWA 离线缓存，独立必需文件）
└── 项目需求与目标总结.md             # 本文档
```
